pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                echo 'Check Image in Azure repo'
            }
        }
       stage('Build Image in Azure repo') {
            steps {
                echo 'Build Image'
            }
        }    
       
    }
}
